@page
@model lab2.Pages.DesertFennels.IndexModel

@using Microsoft.Extensions.Localization
@inject IStringLocalizer<Program> Localizer

@{
    ViewData["Title"] = Localizer["DesertFennels"];
}

<h1>@Localizer["DesertFennels"]</h1>

<p>
    <a asp-page="Create">@Localizer["Create"]</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DesertFennel[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DesertFennel[0].Weight)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DesertFennel[0].Age)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DesertFennel[0].EarLength)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DesertFennel[0].MaxSpeed)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DesertFennel[0].FavouriteWord)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.DesertFennel)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Weight)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Age)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.EarLength)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.MaxSpeed)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.FavouriteWord)
                </td>
                <td>
                    <a asp-page="./Edit" asp-route-id="@item.Id">@Localizer["Edit"]</a> |
                    <a asp-page="./Details" asp-route-id="@item.Id">@Localizer["Details"]</a> |
                    <a asp-page="./Delete" asp-route-id="@item.Id">@Localizer["Delete"]</a>
                </td>
            </tr>
        }
    </tbody>
</table>
